name: Test

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - name: Install uv
      uses: astral-sh/setup-uv@v8.1.0
      with:
        enable-cache: false
    - name: Sync dependencies
      run: uv sync --no-install-project --extra dev
    - name: Build Cython extension
      run: uv run python setup.py build_ext --inplace --force
    - name: Run tests
      run: uv run python -m pytest tests/ -v
